# Thingk0 Tap

## How do I install these formulae?

`brew install thingk0/tap/<formula>`

Or `brew tap thingk0/tap` and then `brew install <formula>`.

Or, in a `brew bundle` `Brewfile`:

```ruby
tap "thingk0/tap"
brew "<formula>"
```

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
